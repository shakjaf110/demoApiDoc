npm or yarn should be install.

run
``` bash
npm install 
```

or 

``` bash
yarn install
```

run 

``` bash
mvn clean install
```

run app as spring boot app

you will find redoc-static.html, open it on browser
